interface:
  display_name: "Daily Work Planner"
  short_description: "Plan focused work sessions before execution."
  default_prompt: "Use $daily-work-planner to turn my available time, files, deadline, and goal into an executable work-session plan."

policy:
  allow_implicit_invocation: true
