interface:
  display_name: "Daily Work Planner"
  short_description: "Plan, checkpoint, resume, and remember work."
  default_prompt: "Use $daily-work-planner to inspect my current task or repo, estimate time, score feasibility, and create an executable work-session plan."

policy:
  allow_implicit_invocation: true
